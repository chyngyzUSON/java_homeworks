1. Что такое интерфейс?
Интерфейс помогает связывать разные объекты или сущности по абстрактным поведением

2. В чем преимущество использования интерфейсов?
При использовании разных, малосвязанных объектов, например как облака, самолет, птицы.

3. Как работают свойства в интерфейсах?
Свойства в интерфейсах поумолчанию являются доступным(public), статичным(static) и окончательным(final),
и необязательно писат public static final перед названием свойства.

4. Может ли один интерфейс унаследовать другой? Каким образом
Да, может наследовать и другие интерфейсы через extends, а также можно наследоваться от многих интерфейсов.

5. Что такое композиция? ("HAS A")
Композиция это жесткий тип отношений или связи между объектами,
например, мотор является частью машины, но не может быть частью другой машины .

5.1. Что такое агрегация? (сам добавил, когда искал ответы на 5-вопрос)
Агрегация это мягкий тип связи между объектами, например, пассажиры могут быть нескольких объектов (Car).

6. Что такое наследование?
Наследование это механизм, который можно создать новый класс на основе (свойств и методов) родительского класса ("IS A").

7. В чем отличие между наследованием и композицией?
При наследовнии создается на основе родителя, а при композиции создается на основе дополнение другого класса.
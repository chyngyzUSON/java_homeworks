1. Как вы понимаете массивы?
хранилище информации одного типа с нумерациями (индексами).

2. Как вы понимаете ArrayList?
обновленная версия массива с новыми возможностями.

3. В чем отличия между обычным массивом и ArrayList?
-у массива ограниченный размер;
-у массива нет методов;

4. Как вы понимаете Wrapper классы и для чего они нужны?
Wrapper классы это специальные классы-близнецы примитивных типов, и они имеют свои методы как все классы

5. Чем отличаются методы set(a, b) и add(a, b) у ArrayList
-add() - добавляет новый элемент в массив и сдвигает последующие элементы вправо;
-set() - добавляет новый элемент в массив и удалает тот элемент на которую он добавляется;